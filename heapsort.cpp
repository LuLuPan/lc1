class Solution {
public:
	vector<int> heapSort(vector<int> &A) {

	}
private:
    void buildMaxHeap(vector<int> &A) {
        size_t n = A.size();
        //A[n/2+1...n]
        for (size_t i = n / 2; i >= 0; i--)
    }
};